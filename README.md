# TypeScript Todo App

This app is a conversion of an existing Todo app built in JSX into TypeScript.

## Installation

- run `npm i`
- run `npm run dev` to view application in developer mode
- run `npm run build` to build the dist folder
